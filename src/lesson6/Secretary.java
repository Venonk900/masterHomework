package lesson6;

public class Secretary {

    void sortDocuments(Document[] documents) {
        for (Document document : documents) {
            if (document.getType().equals("Type1")) {
                //отправка в отдел заказов

            }
        }

    }
}
